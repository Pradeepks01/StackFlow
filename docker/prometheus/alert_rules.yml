groups:
- name: local-alerts
  rules:
  - alert: HighRequestLatency
    expr: http_server_duration_microseconds_sum / http_server_duration_microseconds_count > 100000
    for: 1m
    labels:
      severity: warning
    annotations:
      summary: High latency on {{ $labels.instance }}
