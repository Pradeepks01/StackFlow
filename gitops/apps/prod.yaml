apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: stackflow-prod
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/your-org/stackflow.git
    targetRevision: main
    path: helm/stackflow
    helm:
      valueFiles:
        - values.yaml
        - values-prod.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
