apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "stackflow.fullname" . }}-config
data:
  NODE_ENV: {{ .Release.Namespace | quote }}
  DB_NAME: {{ .Values.postgresql.auth.database | quote }}
  DB_USER: {{ .Values.postgresql.auth.username | quote }}
